{
  "name": "datanexus-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server that mediates DataNexus writes — reads are free, mutations require a reason string that lands in the audit trail.",
  "bin": {
    "datanexus-mcp": "./index.mjs"
  },
  "main": "./index.mjs",
  "scripts": {
    "start": "node ./index.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "zod": "^3.23.8"
  }
}
